// worksheet.js
const ABLE_PDF = {
    download: function() {
        // Trigger the browser's native print engine
        // This captures MathJax symbols perfectly
        window.print();
    }
};